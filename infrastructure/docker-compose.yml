services:
  backend:
    build:
      context: ../backend
    image: rxf-sys/admin-backend:latest
    container_name: rxf-admin-backend
    restart: unless-stopped
    env_file:
      - ./.env
    expose:
      - "8080"
    volumes:
      # Persists the probe-history SQLite file across container restarts.
      # The default STORAGE_DB_PATH points at /data inside the container.
      - rxf-admin-data:/data
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8080/api/health').read()"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s
    networks:
      - admin

  web:
    build:
      context: ../frontend
    image: rxf-sys/admin-web:latest
    container_name: rxf-admin-web
    restart: unless-stopped
    depends_on:
      backend:
        # Don't serve the SPA until the API answers — avoids a window of
        # 502s on /api/* right after `docker compose up`.
        condition: service_healthy
    ports:
      # cloudflared on CT 104 forwards admin.rxf-sys.de -> http://192.168.2.210:80
      - "80:80"
    networks:
      - admin

volumes:
  rxf-admin-data:

networks:
  admin:
    driver: bridge
